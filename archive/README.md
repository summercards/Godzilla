# archive/

历史文件。**不参与构建，不被任何代码引用**，只是不想丢掉而已。

| 文件 | 是什么 | 为什么在这 |
| --- | --- | --- |
| `雨夜新宿-游戏与源码.zip` | 2.94MB，7 个条目 | 更早的街机版：玩家还能操作、有血条和死亡的那一版。`game.js` 31.3KB，与现在的 `tv/game.js` 不是同一条血脉 |

这是 archive 里仅剩的一项。它虽然叫「雨夜新宿」，但**它不是当前挂机版**，
而是那条已经被砍掉的街机分支留下的唯一一份完整可跑记录。留着是为了将来
万一要往回看美术或数值演变的轨迹，但要明确一点 —— **它已经不是这个项目的一部分了**。

要恢复旧的街机版玩法，解开这个 zip 即可，里面是完整的一套
（`index.html` + `game.js` + `style.css` + 原图）。

## 已移除的两项（2026-09-17 工程整理）

| 原文件 | 为什么删 |
| --- | --- |
| `雨夜新宿.html`（4.09MB） | 与 `godzilla-pet/dist/巨兽都市-挂机直播.html` **逐字节相同**，实测 MD5 均为 `4fb906261f8ab0061455415dce82d9e6`，是改名时误留的副本。想拿回当前挂机版，直接用 `dist/` 里那份即可 |
| `巨兽都市-挂机版与源码.zip`（旧版全量打包） | 内容已被仓库目录（`godzilla-pet/tv/`）完全覆盖，且这份文件在整理时已不在磁盘上 —— 本文件与根 `README.md` 当时对它的描述（61.3MB、57 个条目）是过期记录 |

两项都能从 git 历史里取回：它们在被删前都是已提交状态。
